<% include ./../partials/header %>
    <a class="btn btn-primary btn-sm" href="/lma/new">Add / Remove Worker</a>
    <a class="btn btn-primary btn-sm" href="/lma/all">Collective Report</a>
</div>

<main class="row">
    <div class="col-md-12">
        <div class="list-workers">
            <h3></h3>
            <ol>
                <% subWorkers.forEach((foundWorker) => { %>
                <div class="d-flex">
                    <li _id="<%= foundWorker._id %>" > <%= foundWorker.firstname %>&nbsp;<%= foundWorker.surname %> 
                        <form action="/lma" method="POST">
                            <input style="display: none" type="text" name="id" value="<%= foundWorker._id %>" >
                        </form>
                        <a class="btn btn-sm btn-primary" href="/lma/report/<%= foundWorker._id %>">
                            <%= foundWorker.username %>'s Report
                        </a>
                        <a class="btn btn-sm btn-success" href="/lma/prayer/<%= foundWorker._id %>">
                            <%= foundWorker.username %>'s Prayer Report
                        </a>
                    </li>
                </div>
                <hr>
                <% });  %>
            </ol>
            <div class="col-lg-4 col-md-6 col-sm-8 col-xs-10">
            </div>
            <p></p>
            <hr />
            <p></p>
            </br>
        </div>
        <p></p>
    </div>
</main>
<p></p>
<p></p>
<% include ./../partials/footer %>